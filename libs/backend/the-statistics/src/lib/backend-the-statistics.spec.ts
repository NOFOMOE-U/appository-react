import { backendTheStatistics } from './backend-the-statistics'

describe('backendTheStatistics', () => {
  it('should work', () => {
    expect(backendTheStatistics()).toEqual('backend-the-statistics')
  })
})
