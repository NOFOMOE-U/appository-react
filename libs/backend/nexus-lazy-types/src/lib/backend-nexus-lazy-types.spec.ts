import { backendNexusLazyTypes } from './backend-nexus-lazy-types'

describe('backendNexusLazyTypes', () => {
  it('should work', () => {
    expect(backendNexusLazyTypes()).toEqual('backend-nexus-lazy-types')
  })
})
