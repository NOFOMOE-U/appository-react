import { backendCommunication } from './backend-communication'

describe('backendCommunication', () => {
  it('should work', () => {
    expect(backendCommunication()).toEqual('backend-communication')
  })
})
